/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"res.cloudinary.com",
			"onedrive.live.com",
			"1drv.ms",
		],
	},
};
module.exports = nextConfig;
